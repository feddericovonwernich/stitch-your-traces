apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "story-service.fullname" . }}-test-connection"
  labels:
    {{- include "story-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "story-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
